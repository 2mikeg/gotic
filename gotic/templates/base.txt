// This is an autogenerated file, review it before use
// Reviewed by: 
package autogenerated

%for model in models_list:
type ${model.model_name} struct {
    %for item in model.items_mapping:
    %if item.json_name is None:
    ${item.key_to_go} ${item.type_to_go}
    %else:
    %if item.json_name not in model.required_items:
    ${item.key_to_go} ${item.type_to_go} `json:"${item.json_name},omitempty"`
    %else:
    ${item.key_to_go} ${item.type_to_go} `json:"${item.json_name}"`
    %endif
    %endif
    %endfor
}
%endfor
